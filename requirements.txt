# Core runtime for the native desktop app (works on Python 3.14; PySide6 ships
# an abi3 wheel, pyqtgraph is pure-Python).
PySide6>=6.6
pyqtgraph>=0.13
numpy>=1.26
pyperclip>=1.8       # local clipboard access for automatic turn capture
markdown-it-py>=3.0  # render assistant replies as markdown in the chat

# --- optional: in-app chat providers (install the one(s) you use) ---
#   pip install anthropic        # Claude
#   pip install google-genai     # Gemini
#   pip install openai           # OpenAI

# --- optional: semantic (neural) drift - onnxruntime, no torch, downloads once ---
#   pip install fastembed

# --- optional: the Streamlit browser app (`drifter web`) ---
#   pip install streamlit
